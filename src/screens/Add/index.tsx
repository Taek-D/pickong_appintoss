// S-ADD — 풀구현은 01-05 plan에서
export function AddFlow(): JSX.Element {
  return (
    <div className="safe-area flex h-full flex-col items-center justify-center p-6 text-center">
      <h1 className="text-[20px] font-semibold">콩 등록</h1>
      <p className="mt-2 text-[14px] text-[var(--color-text-muted)]">
        (S-ADD 3단계 풀구현은 01-05 plan)
      </p>
    </div>
  );
}
